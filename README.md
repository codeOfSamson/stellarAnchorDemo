# stellarAnchorDemo
## For the FE project: `npm install && npm run dev`
---------------------------------------
## For the BE create a .env with the following:
HOME_DOMAIN=https://anchor-stage.owlpay.com
CLIENT_DOMAIN=your domain
CLIENT_SIGNING_KEY=your signing key
PORT=local port

## then run: `npm install && node server.js`
